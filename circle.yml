machine:
  timezone:
    Asia/Tokyo
  ruby:
    version: 2.1.5

dependencies:
  pre:
    - sudo pip install awscli

checkout:
  post:
    - cp config/secrets.yml.sample config/secrets.yml

deployment:
  staging:
    branch: staging
    commands:
      - sh deploy_staging.sh